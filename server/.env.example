# 后端配置
PORT=8080
# Postgres 连接串（与 db/docker-compose.yml 一致）
DATABASE_URL=postgres://crm:crm@localhost:5432/nextcrm
# 当前登录用户/租户（演示用；生产应来自鉴权中间件）
DEFAULT_ORG_ID=1
DEFAULT_USER_ID=1
# 允许的前端来源（CORS），逗号分隔
CORS_ORIGIN=http://localhost:5173
